{
  "name": "personal-blog-worker",
  "version": "1.0.0",
  "description": "Personal blog deployed on Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "preview": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
